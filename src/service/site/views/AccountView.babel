const AsyncComponent = require('./AsyncComponent.js');

const AccountView = AsyncComponent (props =>  props.accountRepo.getAccount(), state =>
    <span>
      <span className="btn">{state.userName}</span>
      <span className="btn">{state.currentTokens} tokens remaining</span>
    </span>)

module.exports = AccountView;
